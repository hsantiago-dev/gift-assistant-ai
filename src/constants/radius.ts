/**
 * Raio de borda do design system "Candy".
 * - `pill` é usado em botões, chips, badges e inputs (raio completo).
 * - `card` é usado em cards (16–20px).
 */

export const Radius = {
  pill: 999,
  card: 20,
  cardSmall: 16,
} as const;
